# Core app
streamlit>=1.30
pandas>=2.0
numpy>=1.24
plotly>=5.15

# Google Drive (service account) client
google-api-python-client>=2.100.0
google-auth>=2.20.0
google-auth-httplib2>=0.2.0
httplib2>=0.22.0

# EPW parsing (Ladybug Tools)
ladybug

# Optional: enable Parquet downloads (comment out if not needed)
pyarrow>=14.0.0